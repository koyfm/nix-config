## Git Commit Style

When committing, use a short descriptive title followed by a body with
a brief description of the changes. Match the commit style of the project —
if the project uses conventional commits, follow that convention instead.
